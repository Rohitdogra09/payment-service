server:
  port: 8084

spring:
  application:
    name: payment-service

  datasource:
    # url: jdbc:mysql://localhost:3306/hotel_payment_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    # username: root
    # password: your password
    url: jdbc:mysql://shinkansen.proxy.rlwy.net:29442/railway?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: akTsDxnrZgeqGDgwbWbsECyLeWihkCuk
    
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

logging:
  level:
    com.hotel.payment: DEBUG
